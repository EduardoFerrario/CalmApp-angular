import { DisableIfDirective } from './disable-if.directive';

describe('DisableIfDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableIfDirective();
    expect(directive).toBeTruthy();
  });
});
